msgpack
PySide6
# app/media_viewer.py's video-thumbnail extraction (replaced a subprocess
# `ffmpeg` call 2026-09-14). The official macOS PyPI wheel's bundled
# FFmpeg build cannot decode HEVC (confirmed directly: 6/6 real HEVC test
# videos yielded zero frames) -- a from-source build linked against a
# real FFmpeg fixed it there. A from-source Windows build was attempted
# in CI too, but reverted after two real failures (chocolatey package
# fragility, then no pkg-config files in the standard Windows FFmpeg
# distribution) -- CI currently installs the plain wheel and this is
# PENDING a real check of whether the separately-built WINDOWS wheel
# has the same HEVC gap the macOS one does (never confirmed either way).
# See build-windows-exe.yml's own comment and TODO.md item 17.
av
# av.VideoFrame.to_image() (used by _video_frame_bytes above) requires PIL/
# Pillow -- confirmed a real gap during a local PyInstaller smoke build:
# PIL was only present in the dev venv as an incidental transitive
# dependency of something else, was never declared here, and the build
# log showed it EXCLUDED from the frozen bundle as a result ("excluded
# module named PIL - imported by av.video.frame (delayed)") -- a fresh
# `pip install -r requirements.txt` without this line would ship an exe
# where every video thumbnail silently raises ImportError at runtime.
# Declared explicitly so it's always bundled.
Pillow
# app/media_viewer.py's _load_qimage HEIC/HEIF fallback (added 2026-09-14).
# Qt's own QImage.loadFromData() only decodes HEIC when the OS itself
# supplies a codec -- macOS does (confirmed working via its ImageIO
# framework), Windows (this project's primary shipped target) has none by
# default -- confirmed directly, not assumed: grepped ffs_explorer.spec and
# the Windows CI workflow for any bundled HEIC codec, zero hits. Without
# this, every real HEIC photo (Apple's default photo format since iOS 11)
# would silently show as an undecodable placeholder on Windows, both as a
# thumbnail and in the full-size viewer.
pillow-heif
blackboxprotobuf
pytypedstream
nska_deserialize
tzdata; sys_platform == "win32"
pyobjc-framework-Cocoa; sys_platform == "darwin"
# chrome_cache.py (app/chrome_cache.py) decompresses real cached HTTP
# response bodies -- brotli ('br') and zstd are common real-world
# Content-Encoding values (confirmed: 16%/2% of entries respectively on
# real Android 14 JoshHickman casework), not exotic. Lazy-imported only
# inside chrome_cache._decompress_body, same optional-but-listed
# convention as mcp/uvicorn below, so a frozen build still includes them.
brotli
zstandard
# Optional at runtime (AI access / MCP server — lazy-imported), required for
# full builds so the frozen exe includes the feature.
mcp
uvicorn
